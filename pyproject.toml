[project]
name = "uv-wildcard-probe"
version = "0.1.0"
description = "Probe: uv 0.12.4 wildcard version constraints + lockfile markers"
requires-python = ">= 3.8"
dependencies = [
    "anyio == 4.*",
    "click == 8.*",
    "packaging == 24.*",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
